# CLAUDE.md

Este archivo existe únicamente como puente de compatibilidad para agentes que prioricen `CLAUDE.md` por convención (por ejemplo, Claude Code).

Las instrucciones reales del proyecto están en el archivo global `AGENTS.md` de la raíz.

@AGENTS.md
